This directory holds one-time-use / command-specific helper scripts for the `example` subcommand — not part of the routine `zcrypto` CLI flow.
`gen_example_data.py` generates the synthetic example dataset used for development and testing.
Run it with `uv run python cli/example/scripts/gen_example_data.py`.
